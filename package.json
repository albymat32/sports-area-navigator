{
  "name": "nexus-arena-navigator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "start": "vite --port 8080 --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vitest": "^1.4.0",
    "jsdom": "^24.0.0"
  }
}
